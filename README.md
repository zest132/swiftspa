# SwiftSPA

**경량 Web Components 기반 SPA 프레임워크**

SwiftSPA는 **라우팅, 페이지 교체, Web Components, 조건부
렌더링(data-if)** 을 기반으로\
레거시 HTML 프로젝트에서도 쉽게 도입 가능한 초경량 SPA 엔진이다.

가상 DOM 없이 순수 DOM 기반으로 동작하며,\
UI/디자인/애니메이션은 프로젝트가 자유롭게 선택할 수 있도록 완전히
분리한다.

------------------------------------------------------------------------

## 🚀 특징

### ✔ Zero-dependency

외부 라이브러리 없이 실행된다.

### ✔ Web Components 기반

Shadow DOM 컴포넌트를 자연스럽게 활용할 수 있는 구조.

### ✔ 페이지 교체 방식 SPA 라우팅

`/pages/**/index.html` 규칙만 지키면 자동 라우팅 처리.

### ✔ 조건부 렌더링(data-if) 제공

`<div data-if="auth">` 형태로 간단한 렌더링 제어가 가능하다.

### ✔ 확장 가능한 이벤트 훅

`spa:navigate`, `state:changed` 등 이벤트를 통해\
AOS, GSAP, GA4, 로그인 상태 제어 등 다양한 기능을 프로젝트 측에서
자유롭게 확장할 수 있다.

------------------------------------------------------------------------

## 📦 설치 (Git Submodule)

프로젝트 루트에서 아래 명령을 실행:

``` sh
git submodule add https://github.com/zest132/swiftspa.git static/swiftspa
```

프로젝트 구조 예:

    static/
      swiftspa/       ← Git submodule
      pages/
      components/
      styles/

------------------------------------------------------------------------

## 📁 디렉토리 구조

    swiftspa/
     ├─ core/
     │   ├─ spa.js              # SPA 라우터 / navigate() / 내부 링크 인터셉트
     │   ├─ conditional.js      # data-if 조건부 렌더링
     │   ├─ dom.js              # fetchText, queryAllDeep 유틸
     │   ├─ state.js            # 글로벌 상태/훅 (state:changed)
     │   └─ global.js           # 선택적 전역 유틸
     ├─ styles/
     │   └─ reset.css           # 기본 CSS reset
     └─ index.js                # createSpa(), startSpa() public API

------------------------------------------------------------------------

## 🧭 빠른 시작

HTML에서 SwiftSPA를 초기화하는 기본 예제:

``` html
<script type="module">
    import { createSpa, startSpa } from "/swiftspa/index.js";

    // 1) 이벤트 훅 (선택)
    window.addEventListener("spa:navigate", (e) => {
        console.log("Page changed:", e.detail.path);
    });

    // 2) SPA 설정
    createSpa({
        resolvePageUrl: (path, def) => {
            if (path === "/") return "/pages/public/landing/index.html";
            return def(path);
        }
    });

    // 3) 실행
    startSpa();
</script>
```

------------------------------------------------------------------------

## 📍 페이지 구조

SwiftSPA는 기본적으로 다음 규칙을 따른다:

    /pages/<route>/index.html

예시:

    /pages/public/landing/index.html
    /pages/public/reservations/new/index.html
    /pages/admin/index.html

navigate()가 자동으로 HTML 조각을 로드하여\
`#app-main`에 삽입한다.

------------------------------------------------------------------------

## 🎛 조건부 렌더링 (data-if)

HTML:

``` html
<div data-if="auth">로그인한 사용자만 보임</div>
<div data-if="guest">비로그인 사용자 전용</div>
```

JS에서 인증 상태 변경 시:

``` js
window.dispatchEvent(
  new CustomEvent("state:changed", {
    detail: { auth: true }
  })
);
```

SwiftSPA는 자동으로 UI를 갱신한다.

------------------------------------------------------------------------

## 🎨 AOS / GSAP 연동

SwiftSPA는 디자인/애니메이션을 강제하지 않는다.

``` js
window.addEventListener("spa:navigate", () => {
    if (window.AOS) {
        AOS.refreshHard();
    }
});
```

------------------------------------------------------------------------

## 🧪 개발 철학

-   레거시 HTML 프로젝트에서도 자연스럽게 사용 가능\
-   Web Components 표준 기반\
-   UI/디자인은 강제하지 않고 "기능"만 제공\
-   React/Vue처럼 과도한 추상화 없이 필요한 기능만 제공

------------------------------------------------------------------------

## 📄 License

MIT License

------------------------------------------------------------------------

## 🙌 Contributions

버그 제보 및 PR을 환영합니다!
